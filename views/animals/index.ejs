<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>

  <body>
    <%- include("../partials/header.ejs") %>
    <main>
      <div>
        <% for (animal of animals) { %>

        <article>
          <a href="/animals/<%= animal._id %>">
            <h2>
              <%= animal.species %> - <%= animal.extinct ? "Outta There" : "Still Alive" %> |
              Location: <%= animal.location %> | Life Expectancy: <%= animal.lifeExpectancy %> years
            </h2>
          </a>
        </article>

        <% } %>
        
      </div>
    </main>
  </body>
</html>